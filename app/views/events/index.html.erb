<h1>Événements à venir</h1>

<ul>
  <% @events.each do |event| %>
    <li>
      <strong><%= event.title %></strong> - <%= event.location %> (<%= event.price %>€) 
      | <%= link_to "Voir l'événement", event_path(event.id) %>
    </li>
  <% end %>
</ul>
<!DOCTYPE html>
<html lang="fr">
  <head>
    <title>Eventbrite | Pro</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%# Icônes et Polices %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">

    <%# Un seul fichier CSS pour éviter les erreurs Propshaft en Rails 8 %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <%# On affiche la barre de navigation ici pour toutes les pages %>
    <%= render 'shared/navbar' %>

    <%# Zone des messages de succès ou d'erreur %>
    <div class="container mt-4">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show border-0 shadow-sm" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show border-0 shadow-sm" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <%# Contenu principal de chaque page %>
    <main class="flex-grow-1">
      <%= yield %>
    </main>

    <%# Bas de page %>
    <footer class="bg-dark py-4 mt-auto">
      <div class="container text-center text-white">
        <p class="mb-0 small">© 2026 Eventbrite Clone - Design Modern Business</p>
      </div>
    </footer>
  </body>
</html>
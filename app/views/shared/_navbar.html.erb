<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom fixed-top py-2 shadow-sm">
  <div class="container">
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center fw-bolder text-dark" do %>
      <div class="bg-primary rounded-3 d-flex align-items-center justify-content-center me-2 shadow-sm" style="width: 32px; height: 32px;">
        <i class="bi bi-lightning-charge-fill text-white small"></i>
      </div>
      <span class="ls-tight text-uppercase">Event<span class="text-primary">brite</span></span>
    <% end %>

    <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto align-items-center gap-2">
        <li class="nav-item"><%= link_to "Explorer", root_path, class: "nav-link fw-medium text-secondary" %></li>
        
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to new_event_path, class: "btn btn-primary btn-sm px-4 rounded-pill fw-bold shadow-sm transition-up" do %>
              <i class="bi bi-plus-lg me-1"></i> Créer
            <% end %>
          </li>
          <li class="nav-item dropdown ms-lg-2">
            <a class="nav-link p-0 border-0" href="#" id="userDrop" data-bs-toggle="dropdown">
              <div class="avatar-container shadow-custom">
                <%= image_tag "Logo_profils.png", alt: "Profil", class: "avatar-image" %>
              </div>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 rounded-4 p-2 mt-2 animate slideIn">
              <li class="px-3 py-2 small text-muted border-bottom mb-2">Compte : <br><b class="text-dark"><%= current_user.email %></b></li>
              <li><%= link_to "Mon Profil", user_path(current_user.id), class: "dropdown-item rounded-3 py-2" %></li>
              <li><%= link_to "Paramètres", edit_user_registration_path, class: "dropdown-item rounded-3 py-2" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item text-danger rounded-3 py-2" %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item"><%= link_to "Connexion", new_user_session_path, class: "nav-link fw-medium" %></li>
          <li class="nav-item"><%= link_to "S'inscrire", new_user_registration_path, class: "btn btn-dark btn-sm px-4 rounded-pill fw-bold shadow-sm" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>